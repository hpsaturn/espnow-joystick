// A very simple protocol definition, consisting of only
// one message.

syntax = "proto2";

message SimpleMessage {
    required uint32 ax = 1;
    required uint32 ay = 2;
    required uint32 az = 3;
    required uint32 ck = 4;
}

